"use client"
import { useMemo, useState } from "react"
import Link from "next/link"
import { AppShell } from "@/components/shared/AppShell"
import { ContestCard, FilterBar, SectionTitle } from "@/components/contests/ContestUI"
import { contests, type ContestStatus } from "@/lib/contests"

export default function ContestsPage() { const [status, setStatus] = useState<ContestStatus | "All">("All"); const filtered = useMemo(() => status === "All" ? contests : contests.filter((contest) => contest.status === status), [status]); return <AppShell variant="curriculum"><main className="mx-auto max-w-7xl px-5 py-10 md:px-8"><div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div className="max-w-2xl"><p className="text-xs uppercase tracking-[.25em] text-[#00D4FF]">Community arena</p><h1 className="mt-3 text-4xl font-semibold tracking-tight">Contests</h1><p className="mt-3 text-white/55">Compete on quantum circuit challenges, climb the leaderboard, and earn badges.</p></div><Link href="/contests/practice" className="inline-flex shrink-0 items-center justify-center rounded-xl border border-[#F5B942]/40 bg-[#F5B942]/10 px-4 py-2.5 text-sm font-semibold text-[#F5B942] transition hover:border-[#F5B942]/70 hover:bg-[#F5B942]/20">Choose a practice track</Link></div><section className="mt-10"><SectionTitle eyebrow="Now in orbit" title="Active contests" /><div className="mt-5 flex gap-4 overflow-x-auto pb-3">{contests.slice(0, 4).map((contest) => <ContestCard key={contest.id} contest={contest} featured />)}</div></section><div className="mt-12 grid gap-8 lg:grid-cols-[1fr_290px]"><section><SectionTitle title="All contests" action={<FilterBar status={status} setStatus={setStatus} />} /><div className="mt-5 grid gap-4 sm:grid-cols-2">{filtered.map((contest) => <ContestCard key={contest.id} contest={contest} />)}</div></section><aside className="h-fit rounded-2xl border border-[#00D4FF]/20 bg-[#00D4FF]/5 p-5"><p className="text-xs uppercase tracking-[.2em] text-[#00D4FF]">Your stats</p><p className="mt-5 text-3xl font-semibold">3,910 <span className="text-sm font-normal text-white/45">XP</span></p><div className="mt-4 h-2 rounded-full bg-white/10"><div className="h-full w-[72%] rounded-full bg-[#00D4FF]" /></div><p className="mt-2 text-xs text-white/45">Level 8 · Circuit Architect</p><div className="mt-6 grid grid-cols-2 gap-3"><div><p className="text-xl font-semibold text-white">#47</p><p className="text-xs text-white/45">Global rank</p></div><div><p className="text-xl font-semibold text-white">6</p><p className="text-xs text-white/45">Badges earned</p></div></div><a href="/me" className="mt-6 block text-sm text-[#00D4FF]">View full profile →</a></aside></div></main></AppShell> }
