import type { AlgoAnimationConfig } from "./types"
import { DeutschJozsaScene } from "@/components/lab/anim/scenes/DeutschJozsaScene"

const states = [["|0⟩", "|1⟩"], ["(|0⟩ + |1⟩)/√2", "(|0⟩ − |1⟩)/√2"], ["superposition", "superposition"], ["phase hidden", "phase hidden"], ["interference", "interference"], ["|0⟩", "|1⟩"], ["Constant", "Balanced"]]
const titles = ["Start", "Hadamard on both", "Send into the oracle", "Phase kickback", "Hadamard again", "Measure", "Result"]
const narration = ["We begin with an input qubit at |0⟩ and a helper at |1⟩.", "Hadamard gates spread each qubit across both possibilities.", "The superposition enters the black-box oracle U_f.", "The oracle hides its answer as a phase sign, not a bit value.", "A second Hadamard layer makes the hidden phase interfere.", "Measurement collapses the state to a definite answer.", "One query distinguishes constant from balanced."]
export const deutschJozsaAnimation: AlgoAnimationConfig = { id: "deutsch-jozsa", title: "Deutsch–Jozsa", qubits: 2, scene: DeutschJozsaScene, glossary: { superposition: "A state containing multiple possibilities at once.", oracle: "A black box that evaluates the function.", "phase kickback": "A phase change transferred from the helper qubit.", interference: "Amplitudes combining to reinforce or cancel." }, steps: titles.map((title, i) => ({ title, narration: narration[i], why: i === 3 ? "The answer is hidden in the phase." : "Each transformation preserves information while making the pattern easier to read.", circuitHighlight: i === 2 || i === 3 ? "oracle" : i, stateBefore: states[Math.max(0, i - 1)], stateAfter: states[i], visualKind: ["start", "hadamard", "oracle", "phase", "interference", "measure", "result"][i] as never, duration: 1800 })) }
